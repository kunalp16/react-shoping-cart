import React, { Component } from 'react';
import { Route } from 'react-router-dom';

class Login extends Component {
    render() {
        return (
            <div>
                <p>This is login in component loaded</p>
            </div>
        )
    };
}

export default Login;